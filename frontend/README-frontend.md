# Frontend (Vite + React + Tailwind)

## 快速开始
1) 安装依赖
```
cd frontend
npm install
```
2) 开发模式
```
npm run dev
```
3) 构建与预览
```
npm run build
npm run preview
```

## 环境变量
在 `frontend` 目录下创建 `.env.local`（可选）：
```
VITE_API_BASE_URL=http://localhost:8000/api
```


